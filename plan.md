# 프로젝트 기획서: Design-to-Code Assistant

> 디자인 시안 이미지를 분석해서 레이아웃 구조와 스타일 토큰을 추출하고, 실제 렌더링 가능한 코드로 변환해주는 AI 기반 프론트엔드 어시스턴트

---

## 1. 프로젝트 개요

| 항목             | 내용                                                                                                                       |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 프로젝트명       | Design-to-Code Assistant (가칭)                                                                                            |
| 한 줄 소개       | 디자인 시안 이미지를 업로드하면 AI가 레이아웃/스타일을 분석해 Tailwind 기반 코드로 변환하고, 즉시 렌더링까지 보여주는 도구 |
| 목표 기간        | 4주                                                                                                                        |
| 타겟 이직처      | 대기업/큰 서비스 프론트엔드 포지션                                                                                         |
| 핵심 어필 포인트 | 프론트엔드 전반 역량 + AI 활용 능력 + 실무 페인포인트 이해도                                                               |

### 왜 이 프로젝트인가

- 디자이너-개발자 협업 과정에서 반복되는 실제 문제(시안 → 코드 변환 수작업)를 다룸
- AI가 주인공이 아니라 "도구"로 사용되고, 프론트엔드 구현력이 메인으로 드러남
- 이미지 분석, 상태관리, 코드 생성, 라이브 렌더링까지 다양한 기술적 챌린지를 포함

---

## 2. 범위 정의 (Scope)

### 하기로 한 것 (In Scope)

- 이미지 업로드 (드래그앤드롭 포함)
- AI를 통한 레이아웃 구조 분석 (그리드/플렉스 구조, 컴포넌트 영역 구분)
- 색상 팔레트 / 타이포그래피 토큰 추출
- 추출된 정보를 Tailwind CSS 기반 HTML/React 코드로 변환
- 생성된 코드의 실시간 프리뷰 (Sandpack 활용)
- 코드 복사 / 다운로드 기능

### 안 하기로 한 것 (Out of Scope) — 범위 폭주 방지용

- 복잡한 인터랙션(애니메이션, 상태 전환) 복원
- 완벽한 픽셀 단위 일치도
- 다중 페이지/전체 사이트 변환
- 반응형 브레이크포인트 자동 생성 (1개 뷰포트 기준만)
- 사용자 인증/멀티 프로젝트 저장 기능

> 💡 **원칙**: "일부만 되어도 괜찮다. 대신 되는 부분은 확실하게 보여준다."

---

## 3. 핵심 사용자 플로우

```
1. 사용자가 디자인 시안 이미지 업로드
        ↓
2. AI가 이미지 분석 (단계별 진행 상황 표시)
   ① 레이아웃 구조 파악 (섹션/영역 분리)
   ② 색상/타이포 토큰 추출
   ③ 컴포넌트 단위 코드 생성
        ↓
3. 결과 화면: 좌측 코드 / 우측 라이브 프리뷰
        ↓
4. 사용자가 코드 복사 또는 다운로드
```

---

## 4. 기술 스택

| 영역          | 기술                           | 선택 이유                                     |
| ------------- | ------------------------------ | --------------------------------------------- |
| 프레임워크    | Next.js 14 (App Router)        | SSR/RSC 경험 어필, 대기업 채용 선호도 높음    |
| 언어          | TypeScript                     | 타입 안정성, 실무 표준                        |
| 스타일링      | Tailwind CSS                   | 생성 코드와 개발 환경 스택 일치, 유지보수성   |
| 상태관리      | Zustand + TanStack Query       | 클라이언트 상태 / 서버(AI) 상태 명확히 분리   |
| AI            | Claude API (Vision)            | 이미지 분석 + 구조화된 출력에 적합            |
| 코드 표시     | react-syntax-highlighter       | 결과 코드 가독성 있게 표시                    |
| 라이브 프리뷰 | Sandpack (CodeSandbox)         | 생성 코드를 즉시 렌더링, 시각적 임팩트 극대화 |
| 테스트        | Vitest + React Testing Library | 핵심 로직 단위 테스트                         |
| CI/CD         | GitHub Actions                 | 린트/테스트/빌드 자동화                       |
| 배포          | Vercel                         | Next.js와 궁합, 빠른 배포                     |

---

## 5. AI 분석 파이프라인 설계 (핵심 난이도)

한 번에 "이미지 → 완성 코드"로 요청하면 품질이 낮아지므로, **3단계로 분리**합니다.

### Step 1. 레이아웃 구조 분석

- 입력: 이미지
- 출력: JSON 형태의 구조 트리 (예: `header > nav`, `main > section(grid, 3 columns)` 등)
- 목적: 코드 생성 전에 "구조"를 먼저 확정해서 정확도를 높임

### Step 2. 디자인 토큰 추출

- 입력: 이미지
- 출력: 주요 색상 팔레트(hex), 폰트 크기/굵기 추정값, 여백 패턴
- 목적: Tailwind 설정과 매핑 가능한 토큰화

### Step 3. 코드 생성

- 입력: Step 1 + Step 2의 결과 (JSON)
- 출력: Tailwind 클래스가 적용된 React 컴포넌트 코드
- 목적: 구조/스타일이 이미 정리된 상태에서 생성하므로 일관성 있는 코드 산출

> 이 3단계 파이프라인 자체가 "AI를 무작정 쓰지 않고, 문제를 분해해서 설계했다"는 근거가 되어 면접에서 좋은 이야깃거리가 됩니다.

---

## 6. 4주 로드맵

### 1주차 — 기반 다지기

- [ ] 프로젝트 셋업 (Next.js, TypeScript, Tailwind, ESLint/Prettier)
- [ ] 업로드 UI 구현 (드래그앤드롭, 미리보기)
- [ ] 전체 레이아웃/라우팅 구조 설계
- [ ] GitHub Actions 기본 CI 세팅 (린트/빌드)

### 2주차 — AI 연동

- [ ] Claude Vision API 연동
- [ ] 3단계 분석 파이프라인 프롬프트 설계 및 테스트
- [ ] 로딩 상태 UX (단계별 진행 표시)
- [ ] 에러 핸들링 (분석 실패, 타임아웃, 재시도 로직)

### 3주차 — 코드 생성 & 프리뷰

- [ ] JSON → 코드 변환 로직
- [ ] Sandpack으로 라이브 프리뷰 구현
- [ ] 코드 복사/다운로드 기능
- [ ] 결과 화면 UI 다듬기

### 4주차 — 마무리 & 포트폴리오화

- [ ] 핵심 로직 단위 테스트 작성
- [ ] 성능 점검 (Lighthouse, 번들 사이즈 체크)
- [ ] README 작성 (의사결정 기록 포함 — 아래 8번 항목 참고)
- [ ] Vercel 배포
- [ ] 포트폴리오/이력서용 프로젝트 설명 문구 정리

---

## 7. 예상 리스크 & 대응 방안

| 리스크                                  | 대응 방안                                                   |
| --------------------------------------- | ----------------------------------------------------------- |
| AI 분석 결과가 부정확하거나 일관성 없음 | 3단계 파이프라인으로 분해, 프롬프트에 예시(few-shot) 포함   |
| 생성된 코드가 실제로 렌더링 안 됨       | Sandpack 프리뷰 전 코드 유효성 검증 단계 추가               |
| API 응답 지연으로 UX 저하               | 스트리밍 응답 + 단계별 로딩 인디케이터로 체감 대기시간 감소 |
| 범위가 계속 늘어남 (Scope Creep)        | 3번 "안 하기로 한 것" 리스트를 주기적으로 확인              |

---

## 8. README에 남길 "의사결정 기록" 초안

면접관이 가장 흥미로워하는 부분이므로 미리 뼈대를 잡아둡니다.

- **왜 3단계 파이프라인으로 나눴는가**: 단일 프롬프트 대비 정확도/디버깅 용이성 비교
- **왜 Sandpack을 선택했는가**: iframe 직접 구현 대비 트레이드오프
- **왜 Zustand + TanStack Query 조합인가**: 클라이언트 상태와 서버 상태 분리의 이점
- **범위를 어떻게, 왜 좁혔는가**: 완벽한 변환 대신 "구조+토큰 추출"에 집중한 이유
- **가장 어려웠던 기술적 문제와 해결 과정** (실제 진행하면서 채워나가기)

---

## 9. 포트폴리오/이력서 서술 예시 (완성 후 다듬을 초안)

> "디자인 시안 이미지를 분석해 Tailwind 기반 코드로 변환하는 AI 어시스턴트를 개발. 단일 프롬프트 방식 대비 정확도를 높이기 위해 레이아웃 분석-토큰 추출-코드 생성 3단계 파이프라인을 설계했으며, Sandpack을 활용한 라이브 프리뷰로 생성 결과를 즉시 검증 가능하도록 구현. Lighthouse 성능 점수 OO점 달성, 핵심 로직 단위 테스트 커버리지 OO% 확보."

---
